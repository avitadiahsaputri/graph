"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TestPlanner = void 0;
/**
 * A planner used for testing.
 */
class TestPlanner {
    constructor(plan, promptResponse) {
        this.plan = plan || {
            type: 'plan',
            commands: [{ type: 'SAY', response: 'Hello' }]
        };
        this.promptResponse = promptResponse;
    }
    plan;
    promptResponse;
    completePrompt(context, state, prompt, options) {
        return Promise.resolve(this.promptResponse);
    }
    generatePlan(context, state, prompt, options) {
        return Promise.resolve(this.plan);
    }
}
exports.TestPlanner = TestPlanner;
//# sourceMappingURL=TestPlanner.js.map